# Research-Nexus Backend - Zero-Docker Requirements
# No Docker needed - uses SQLite + NumPy instead

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Graph Database (Local)
networkx==3.2.1

# Vector Storage (Local)
numpy==1.26.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0

# Note: No Neo4j, No Qdrant - pure Python solution!
# All data stored locally in SQLite + JSON files
